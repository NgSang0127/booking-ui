import { createTheme } from "@mui/material";

const redTheme = createTheme({
    palette: {
        mode: "light",
        primary: {
            main: "#E83350",

        },
        secondary: {
            main: "#EAF0F1",
        },


    },
});

export default redTheme;